### 排查步骤

先确认 **三件事**：

1. `hermes gateway run` 起没起
2. 端口 `8642` 通不通
3. `api_server.extra.key` 跟 `config.json` 的 `hermes.apiKey` 一不一致

---

> 注意：第 3 条最容易踩，两边都是手填的。

改完跑一下 [健康检查](http://127.0.0.1:29998/health) 就知道了。

- 通了就是配置问题
- 还不通就是 gateway 压根没起

用 `curl` 试试也行，*一秒出结果*。
